# responsive_ui_mediaquery

A minimal project that showcases how `MediaQuery` can be used to create responsive UIs, showing whether the device is a phone, tablet or desktop based on the device's screen size width.

<br/>

## Run

Use `flutter run -d chrome --hot` (or use `./run_web.sh` provided script) to run the app.

<br/>
