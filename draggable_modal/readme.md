# draggable_modal

A minimal example of using a draggable modal in Flutter.

The goal was to test how it works on the current version of Flutter (that is 3.41 at the time of this writing, Feb 2026) and on the Web.

Run it using `flutter run -d chrome -hot`
