# flutter_go_router_example

An example of using go_router in Flutter, with a focus on the Web, so that it uses clean (not hashed) URL paths, and the nested routes (like /settings/profile) work as expected (also on refresh).
